{
    "settings": {
        "VirtoCommerce.Search.AzureSearch": {
            "TokenFilter": {
                "title": "Token filter",
                "description": "Choose the token filter to apply when indexing searchable fields. This requires rebuilding the index"
            },
            "NGramTokenFilter": {
                "MinGram": {
                    "title": "Minimum gram length",
                    "description": "The minimum number of characters in a gram. This requires rebuilding the index"
                },
                "MaxGram": {
                    "title": "Maximum gram length",
                    "description": "The maximum number of characters in a gram. This requires rebuilding the index"
                }
            }
        }
    }
}